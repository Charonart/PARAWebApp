export enum FolderType {
    PROJECT = 'PROJECT',
    AREA = 'AREA',
    RESOURCE = 'RESOURCE',
    ARCHIVE = 'ARCHIVE'
}

export enum FolderStatus {
    ACTIVE = 'ACTIVE',
    INACTIVE = 'INACTIVE'
}

export enum TaskStatus {
    TODO = 'TODO',
    IN_PROGRESS = 'IN_PROGRESS',
    DONE = 'DONE',
    CANCELLED = 'CANCELLED'
}
